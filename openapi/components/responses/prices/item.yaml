description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: boolean
          default: true
        result:
          title: Price
          allOf:
            - $ref: ../../schemas/price.yaml