description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: boolean
          default: true
        result:
          title: Product
          allOf:
            - $ref: ../../schemas/product.yaml