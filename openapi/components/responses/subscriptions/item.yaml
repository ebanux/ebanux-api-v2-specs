description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: boolean
          default: true
        result:
          title: Subscription
          allOf:
            - $ref: ../../schemas/subscription.yaml