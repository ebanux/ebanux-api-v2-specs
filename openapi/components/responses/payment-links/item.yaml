description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: boolean
          default: true
        result:
          title: Payment-Link
          allOf:
            - $ref: ../../schemas/payment_link.yaml