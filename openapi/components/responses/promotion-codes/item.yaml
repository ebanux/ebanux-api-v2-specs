description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: boolean
          default: true
        result:
          title: Promotion-Code
          allOf:
            - $ref: ../../schemas/promotion_code.yaml